% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node_edge_weights.R
\name{node_weights}
\alias{node_weights}
\title{Tabulate node-level traversal weights}
\usage{
node_weights(x, method = "all")
}
\arguments{
\item{x}{An \code{igraph} directed acyclic graph \strong{or} a data frame /
matrix whose first two columns are the edge endpoints (\code{from},
\code{to}).}

\item{method}{Character vector; one or more of \code{"SPC"}, \code{"SPLC"},
\code{"SPNP"}, or \code{"all"} (default). Case-insensitive.}
}
\value{
A data frame with columns \code{name} and one column per
requested weight (\code{SPC}, \code{SPLC}, \code{SPNP}).
}
\description{
Computes the node-level analogue of the SPC, SPLC, and SPNP traversal
weights: the number of source-to-sink paths passing \strong{through} each
vertex, rather than across a single edge. Using the same forward/backward
path counts described in \code{\link[=traversal_weights]{traversal_weights()}} — \eqn{f(v)}, \eqn{f_a(v)},
\eqn{b(v)}, \eqn{b_a(v)} — the node-level weights are:
\deqn{SPC(v)  = f(v)   \cdot b(v)}
\deqn{SPLC(v) = f_a(v) \cdot b(v)}
\deqn{SPNP(v) = f_a(v) \cdot b_a(v)}

This is the direct generalization of the edge formula
\eqn{SPC(i \to j) = f(i) \cdot b(j)} to a single vertex (setting
\eqn{i = j = v}), and counts every source-to-sink path that visits
\eqn{v}, regardless of which edge it uses to arrive or leave.
}
\examples{
library(igraph)

el <- data.frame(
  from = c(1, 2, 2, 3, 4),
  to   = c(2, 3, 4, 5, 5)
)
node_weights(el)
node_weights(el, method = "SPNP")

}
\references{
Hummon, N. P., & Doreian, P. (1989). Connectivity in a citation network:
The development of DNA theory. \emph{Social Networks}, \strong{11}(1), 39–63.
\doi{10.1016/0378-8733(89)90017-8}
}
\seealso{
\code{\link[=edge_weights]{edge_weights()}} for the edge-level table,
\code{\link[=traversal_weights]{traversal_weights()}} for the underlying edge-level computation.
}
