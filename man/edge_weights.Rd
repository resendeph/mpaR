% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node_edge_weights.R
\name{edge_weights}
\alias{edge_weights}
\title{Tabulate edge-level traversal weights}
\usage{
edge_weights(x, method = "all")
}
\arguments{
\item{x}{An \code{igraph} directed acyclic graph \strong{or} a data frame /
matrix whose first two columns are the edge endpoints (\code{from},
\code{to}).}

\item{method}{Character vector; one or more of \code{"SPC"}, \code{"SPLC"},
\code{"SPNP"}, or \code{"all"} (default). Case-insensitive.}
}
\value{
A data frame with columns \code{from}, \code{to}, and one column
per requested weight (\code{SPC}, \code{SPLC}, \code{SPNP}).
}
\description{
Computes the SPC, SPLC, and/or SPNP traversal weights for every edge in a
directed acyclic graph (via \code{\link[=traversal_weights]{traversal_weights()}}) and returns them as a
tidy data frame, one row per edge — handy for inspecting or exporting the
raw weights without working with \code{igraph} edge attributes directly.
}
\examples{
library(igraph)

el <- data.frame(
  from = c(1, 2, 2, 3, 4),
  to   = c(2, 3, 4, 5, 5)
)
edge_weights(el)
edge_weights(el, method = "SPC")

}
\references{
Hummon, N. P., & Doreian, P. (1989). Connectivity in a citation network:
The development of DNA theory. \emph{Social Networks}, \strong{11}(1), 39–63.
\doi{10.1016/0378-8733(89)90017-8}
}
\seealso{
\code{\link[=traversal_weights]{traversal_weights()}} to attach weights as edge attributes on the
graph itself, \code{\link[=node_weights]{node_weights()}} for the node-level analogue.
}
