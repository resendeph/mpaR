% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_citation_network.R
\name{build_citation_network}
\alias{build_citation_network}
\title{Build a citation network from fetched bibliographic data}
\usage{
build_citation_network(data, closed = TRUE)
}
\arguments{
\item{data}{A data frame as returned by \code{\link[=fetch_openalex]{fetch_openalex()}}, containing at
least the columns \code{id} and \code{referenced_works}.}

\item{closed}{Logical. If \code{TRUE} (default), only citation links between
papers present in \code{data} are included, producing a self-contained
network. If \code{FALSE}, all referenced works become nodes even if they
do not appear in \code{data}.}
}
\value{
A directed \code{igraph} object. Vertex attributes include
\code{name} (the paper ID), \code{title}, \code{doi}, \code{year},
\code{first_author}, \code{journal}, and \code{cited_by_count} where
available.
}
\description{
Converts a data frame of scholarly works (as returned by \code{\link[=fetch_openalex]{fetch_openalex()}}
or similar functions) into a directed \code{igraph} object representing the
citation network.

An edge A \eqn{\to} B means "paper A cites paper B". Under mpaR's
citation-network convention (see \code{\link[=classify_nodes]{classify_nodes()}}):
\itemize{
\item Old foundational papers accumulate in-edges and become
\code{"source"} nodes.
\item Recent papers not yet cited by others become \code{"terminal"} nodes.
}

After building the network, pass it to \code{\link[=check_dag]{check_dag()}} to verify acyclicity,
then to \code{\link[=traversal_weights]{traversal_weights()}} and \code{\link[=mpa]{mpa()}} to run Main Path Analysis.
}
\examples{
\dontrun{
papers <- fetch_openalex("main path analysis", n = 200)
g_raw  <- build_citation_network(papers)

# Check for cycles and remove if needed
check_dag(g_raw)
g_dag <- igraph::delete_edges(g_raw, mpaR:::.find_back_edges(g_raw))

# Run MPA
g      <- traversal_weights(g_dag)
result <- mpa(g)
plot_mpa(g, result$global, scope = "full")
}

}
\seealso{
\code{\link[=fetch_openalex]{fetch_openalex()}}, \code{\link[=check_dag]{check_dag()}}, \code{\link[=traversal_weights]{traversal_weights()}}, \code{\link[=mpa]{mpa()}}
}
