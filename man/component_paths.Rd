% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component_paths.R
\name{component_paths}
\alias{component_paths}
\title{Extract main paths from every component of a disconnected network}
\usage{
component_paths(
  x,
  type = c("global", "local", "key_route"),
  weight = c("SPC", "SPLC", "SPNP"),
  k = 1L,
  min_size = 3L
)
}
\arguments{
\item{x}{An \code{igraph} DAG or a data frame / matrix edge list.}

\item{type}{Main-path extraction strategy passed to \code{\link[=main_path]{main_path()}}:
\code{"global"} (default), \code{"local"}, or \code{"key_route"}.}

\item{weight}{Traversal weight: \code{"SPC"} (default), \code{"SPLC"}, or
\code{"SPNP"}.}

\item{k}{Number of key-route seed edges per component. Only used when
\code{type = "key_route"}. Defaults to \code{1L}.}

\item{min_size}{Integer. Components with fewer than \code{min_size} vertices
are excluded. Defaults to \code{3L}.}
}
\value{
A named list of \code{igraph} subgraphs, one per retained component,
named \code{"component_1"}, \code{"component_2"}, etc. (ordered by
decreasing component size). Each subgraph contains only the main-path
vertices and edges for that component, with the traversal weight as an
edge attribute.

The full decomposition summary is attached as attribute
\code{"component_summary"}: a data frame with columns
\code{component}, \code{n_vertices}, \code{n_edges}, and
\code{retained}.
}
\description{
Real-world citation networks (e.g. cross-sector patent networks) often
consist of many weakly connected components rather than a single giant
component. \code{component_paths()} decomposes the network into its weakly
connected components, runs \code{\link[=traversal_weights]{traversal_weights()}} and \code{\link[=main_path]{main_path()}} on each
one independently, and returns the combined result.

Components smaller than \code{min_size} vertices are silently dropped,
keeping only technologically significant sub-networks. Inspired by the
key-route component-selection strategy of Liu et al. (2019).
}
\examples{
library(igraph)

# Two disconnected chains: 1->2->3  and  4->5->6->7
el <- data.frame(
  from = c(1, 2, 4, 5, 6),
  to   = c(2, 3, 5, 6, 7)
)

paths <- component_paths(el, type = "global", weight = "SPC")
length(paths)          # 2 components
attr(paths, "component_summary")

# Inspect the larger component
igraph::as_edgelist(paths[["component_1"]])

}
\references{
Liu, J. S., Chen, H. H., Ho, M. H. C., & Li, Y. C. (2012). Citations
networks as a research tool. \emph{Journal of the American Society for
Information Science and Technology}, \strong{63}(9).

Liu, P., Guo, Q., Yet, F., & Chen, X. (2019). Few-shot learning with
key-route main paths. \emph{Scientometrics}, \strong{121}(3), 1437–1451.
}
